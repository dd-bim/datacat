package de.bentrm.datacat.catalog.domain.Enums;

public enum XtdDataTypeEnum {
    XTD_BOOLEAN,
    XTD_INTEGER,
    XTD_RATIONAL, // Datatype to specify a number that can be expressed as the fraction of two integers.
    XTD_REAL,
    XTD_COMPLEX, // Datatype to specify a number that can be expressed in the form a+bi, where a and b are real numbers, and i is the imaginary unit.
    XTD_STRING,
    XTD_DATETIME
}
